$(function () {
    var t = $("#contact-form");
    t.submit(function () {
        if (t.valid()) {
            t.find(".submit-area").addClass("loading");
            var e = $(this).serialize();
            $.post($(this).attr("action"), e, function () {
                setTimeout(function () {
                    hideLoader(), t.find(".submit-area").removeClass("loading").addClass("success"), t.clearForm()
                }, 1e3)
            })
        }
        return !1
    })
}), $.fn.clearForm = function () {
    return this.each(function () {
        var t = this.type,
            e = this.tagName.toLowerCase();
        return "form" == e ? $(":input", this).clearForm() : void("text" == t || "password" == t || "textarea" == e ? this.value = "" : "checkbox" == t || "radio" == t ? this.checked = !1 : "select" == e && (this.selectedIndex = -1))
    })
};